[project]
name = "metagoofil"
version = "1.0"
dependencies = [
"google",
"requests",
"argparse",
]

[build-system]
requires = [
    "setuptools",
    "setuptools-scm"
]

[tool.setuptools.package-data]
mypkg = ["user_agentes.txt"]

[project.scripts]
metagoofil = "metagoofil:main"